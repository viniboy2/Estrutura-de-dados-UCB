#include<stdio.h>
#include<stdlib.h>
int fatorial  (int N);
int main(){
    system("cls");
    int N =3;
     int F =f(n);
     if(F!=ERRO) printf("f(%d)=%d.\n",N,F);
     else printf{
        printf("ERRO F SÓ Calcula inteiro!= de 0 ");

     }
}
int f (int x){
    if (x ==0 )return (3);
    else if (x>0)return (2*f(x-1)+5);
    else return 0;
}
// fução recurciva 
